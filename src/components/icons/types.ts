import { ReactNode } from "react";

export type IconWrapperProps = {
  children: ReactNode;
  className: string;
  height?: number,
  width?: number
}

export type IconProps = {
  height?: number,
  width?: number
}